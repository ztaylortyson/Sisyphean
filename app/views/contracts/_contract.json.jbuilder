json.extract! contract, :id, :source, :kind, :year, :Member_id, :notes, :ee_sign, :ee_signed, :hr_signed, :gsm_signed, :gm_signed, :dir_op_signed, :effective_date, :draw, :salary, :hourly, :duplicate, :created_at, :updated_at
json.url contract_url(contract, format: :json)
